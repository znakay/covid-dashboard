import './Header.css';

const Header = () => {
    return (
        <header className="header">
            <h1 className="header-title">COVID-19 Dashboard</h1>
        </header>
    );
};

export default Header;